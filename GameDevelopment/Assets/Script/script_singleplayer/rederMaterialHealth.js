#pragma strict
var timer=0.0;
public var healthBar : UnityEngine.UI.Image;
static var x=0;

/*

function Start () {
//renderer.material.color.a = 1;
}

function Update () {
	//timer = timer + Time.deltaTime;
	if(PlayerHealth.amountfill>0.9){
healthBar.color.g=255;
healthBar.color.r=0;


}
//	if(timer >= 0)
	//{
	
	if(PlayerHealth.on==1){
	
	
//	if(PlayerHealth.amountfill<0.6){//&&PlayerHealth.amountfill>0.5){// && PlayerHealth.y==x)
		healthBar.color.r+=0.3; //0.6* Time.deltaTime;
		
		//x=+1;
	
	
	//PlayerHealth.on=0;
	//}
	
	if(PlayerHealth.amountfill>=0.6){
	PlayerHealth.onstop=1;
	
	}
	
	}
	if(PlayerHealth.on==2){
	
	
//	if(PlayerHealth.amountfill<0.6){//&&PlayerHealth.amountfill>0.5){// && PlayerHealth.y==x)
		healthBar.color.g-=0.3; //0.6* Time.deltaTime;
		
		//x=+1;
	
	
	//PlayerHealth.on=0;
	//}
	
	if(PlayerHealth.amountfill<=0.6){
	PlayerHealth.onstop=0;
	
	}
	
	}
	
	
	//	healthBar.color.r -= 0.6 * Time.deltaTime;
	//}
	
}*/